# Devlog

## Unreleased

### Mobile board threads

- Added local draft and search state to the board tab so users can start and filter conversations without leaving the screen.
- Hydrated the thread detail route from navigation params when a board thread is created client-side, including ticker logo lookup for matching holdings.

### Mobile settings refresh

- Reworked the settings tab into grouped sections with reusable wrappers for inputs, segmented controls, and summary pills.
- Added a profile summary header and isolated onboarding reset actions in a dedicated danger zone card.
