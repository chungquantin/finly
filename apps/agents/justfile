set shell := ["bash", "-euo", "pipefail", "-c"]

default:
  @just --list

start:
  if [[ -x .venv/bin/finly-agent-server ]]; then .venv/bin/finly-agent-server; elif [[ -x .venv/bin/python ]]; then .venv/bin/python main.py; else python3 main.py; fi

server: start
